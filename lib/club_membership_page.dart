import 'package:flutter/material.dart';

class ClubMembershipPage extends StatelessWidget {
  const ClubMembershipPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Club Membership'),
      ),
      body: const Center(
        child: Text('Club Membership Page'),
      ),
    );
  }
}
